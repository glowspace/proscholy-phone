include: package:flutter_lints/flutter.yaml

linter:
  rules:

analyzer:
  exclude:
    # ignore warnings in files from json_serializable, riverpod_generator
    - "**/*.g.dart"
    # ignore warnings in files generated by freezed specifically.
    - "**/*.freezed.dart"
  errors:
    invalid_annotation_target: ignore
