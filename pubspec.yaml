name: zpevnik

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  jaguar_orm:
  jaguar_query_sqflite:
  sqflite:
  path:

  shared_preferences:
    
  provider:

  modal_bottom_sheet:

  cupertino_icons:

dev_dependencies:
  jaguar_orm_gen:
  build_runner:

flutter:
  uses-material-design: true

  assets:
    - assets/data.json
    - assets/images/
    - assets/images/songbooks/
    - assets/icons/
